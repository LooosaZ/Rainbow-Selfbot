import { __decorate } from "tslib";
import { Enumerable } from '@d-fischer/shared-utils';
import { rtfm } from 'twitch-common';
/**
 * A chat emote.
 */
var ChatEmote = /** @class */ (function () {
    /** @private */
    function ChatEmote(data) {
        this._data = data;
    }
    Object.defineProperty(ChatEmote.prototype, "id", {
        /**
         * The emote ID.
         */
        get: function () {
            return this._data.id;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(ChatEmote.prototype, "code", {
        /**
         * The emote code, i.e. how you write it in chat.
         */
        get: function () {
            return this._data.code;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(ChatEmote.prototype, "setId", {
        /**
         * The ID of the emote set.
         */
        get: function () {
            return this._data.emoticon_set;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Builds the URL pointing to the emote image.
     *
     * @param size The pixel density of the emote image.
     */
    ChatEmote.prototype.getUrl = function (size) {
        return "https://static-cdn.jtvnw.net/emoticons/v1/" + this.id + "/" + size;
    };
    __decorate([
        Enumerable(false)
    ], ChatEmote.prototype, "_data", void 0);
    ChatEmote = __decorate([
        rtfm('twitch', 'ChatEmote', 'id')
    ], ChatEmote);
    return ChatEmote;
}());
export { ChatEmote };
